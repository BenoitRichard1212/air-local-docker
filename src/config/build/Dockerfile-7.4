FROM quay.io/45air/wordpress:7.4-nginx-fpm

ARG WP_ENV
ARG TEMPLATE_PHP_INI
ARG PHP_EXTENSIONS
ENV WP_ENV ${WP_ENV}
ENV TEMPLATE_PHP_INI ${TEMPLATE_PHP_INI}
ENV PHP_EXTENSIONS ${PHP_EXTENSIONS}

USER root

RUN install-php-extensions ${PHP_EXTENSIONS}

USER docker
