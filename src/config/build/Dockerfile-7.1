FROM gcr.io/air-cloud-public/wordpress:7.1-nginx-fpm-dev

ARG NODE_VERSION
ARG WORDPRESS_VERSION
ARG PHP_EXTENSIONS
ARG TEMPLATE_PHP_INI
ARG WP_ENV

ENV NODE_VERSION ${NODE_VERSION}
ENV WORDPRESS_VERSION ${WORDPRESS_VERSION}
ENV PHP_EXTENSIONS ${PHP_EXTENSIONS}
ENV TEMPLATE_PHP_INI ${TEMPLATE_PHP_INI}
ENV WP_ENV ${WP_ENV}
